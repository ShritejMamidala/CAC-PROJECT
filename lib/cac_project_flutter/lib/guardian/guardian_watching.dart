import 'package:flutter/material.dart';

class GuardianWatchingPage extends StatelessWidget {
  const GuardianWatchingPage({super.key});
  @override
  Widget build(BuildContext context) => const Center(
      child: Text('Guardian Watching', style: TextStyle(fontSize: 22)));
}
