import 'package:flutter/material.dart';

class BlindCameraPage extends StatelessWidget {
  const BlindCameraPage({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Blind Camera', style: TextStyle(fontSize: 22)));
}
