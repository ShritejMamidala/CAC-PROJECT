import 'package:flutter/material.dart';

class BlindGPSPage extends StatelessWidget {
  const BlindGPSPage({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Blind GPS', style: TextStyle(fontSize: 22)));
}
